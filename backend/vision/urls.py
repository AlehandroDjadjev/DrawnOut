# vision/urls.py
"""
URL patterns for the vision app (SigLIP2 embedding endpoints).
Currently empty - services are called internally, not via HTTP API.
"""
from django.urls import path

urlpatterns = [
    # No HTTP endpoints for now - SigLIP2 is used internally by lesson_pipeline
]




