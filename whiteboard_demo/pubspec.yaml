name: whiteboard_demo
description: Minimal white whiteboard with upload-and-place
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  opencv_dart: ^1.4.3  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  file_picker: ^8.0.0
  opencv_dart: ^1.4.3 

flutter:
  uses-material-design: true
